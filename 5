#include <iostream>

int main() {
    int k;
    std::cout << "Введіть номер варіанту: ";
    std::cin >> k;

    for (int i = 1; i <= 10; ++i) {
        std::cout << k << " x " << i << " = " << k * i << std::endl;
    }

    return 0;
}
